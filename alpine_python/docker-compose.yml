version: '3.8'

services:
  app:
    container_name: ${CONTAINER_NAME} 
    build:
      context: .
    volumes:
      - ./data:/${DIRECTORY}
    ports:
      - "${SSH_PORT}:22"
    restart: always
          
    